const Number=['one','two','three','four','five']
//Number.shift()
console.log(Number);


// Using unshift we can add a string in the front of the Array
// Number.unshift('hello')
// console.log(Number);

// push method is used to add the string at last.
// Number.push('six')
// console.log(Number)

// push method is used to add the string at last.
// Number.pop()
// console.log(Number)

Number.splice(2,1,'Something')
console.log(Number);
